twig:
    file_name_pattern: '*.twig'
   

    default_path: '%kernel.project_dir%/templates'
    paths:
        '%kernel.project_dir%/src/Contacts/UI/Templates': 'Contacts'
        '%kernel.project_dir%/src/BackUsers/UI/Templates': 'BackUser'
        
when@test:
    twig:
        strict_variables: true
