api_platform:
    mapping:
        paths: 
            - '%kernel.project_dir%/src/Entity/'
            - '%kernel.project_dir%/src/Categories/Domain/Entity/'

    formats:        
        json: ['application/json']   
        jsonld: ['application/ld+json']    

    title: Hello API Platform
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
