Exemple de contrôleur Symfony correct
php

#[Route('/seller/order-items/{id}/accept', methods: ['POST'])]
public function accept(int $id, Request $request): JsonResponse
{
    $token = $request->query->get('token');

    // Vérification du token, de l'utilisateur, etc.
    // Mise à jour de la commande

    return new JsonResponse(['status' => 'ok'], 200);
}

Même chose pour le refus :
php

#[Route('/seller/order-items/{id}/refuse', methods: ['POST'])]
public function refuse(int $id, Request $request): JsonResponse
{
    $token = $request->query->get('token');

    // Vérification du token, etc.

    return new JsonResponse(['status' => 'ok'], 200);
}
